[pytest]
markers =
    large: marks tests that upload large files (621 MB) to the VPS inference server (slow, network-intensive)
    slow: marks tests that wait for long-running CPU inference to complete (30+ minutes)
